<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Queries for camera intent visibility (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera and Storage permissions -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HikeNativeApp"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- FileProvider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.HikeNativeApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp.NoActionBar" />

        <!-- Hike List Activity -->
        <activity
            android:name=".ui.hike_list.HikeListActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Add Hike Activity -->
        <activity
            android:name=".ui.add_hike.AddHikeActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Confirm Hike Activity -->
        <activity
            android:name=".ui.confirm_hike.ConfirmHikeActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Edit Hike Activity -->
        <activity
            android:name=".ui.edit_hike.EditHikeActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Observation List Activity -->
        <activity
            android:name=".ui.observation_list.ObservationListActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Add Observation Activity -->
        <activity
            android:name=".ui.add_observation.AddObservationActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Edit Observation Activity -->
        <activity
            android:name=".ui.edit_observation.EditObservationActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Chatbot Activity -->
        <activity
            android:name=".ui.chatbot.ChatbotActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- Report Activity -->
        <activity
            android:name=".ui.report.ReportActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

        <!-- User Settings Activity -->
        <activity
            android:name=".ui.user_settings.UserSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.HikeNativeApp" />

    </application>

</manifest>
